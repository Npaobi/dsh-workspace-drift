# 项目环境体检助手：插件开发清单

目标：做一个 DSH 插件，帮助用户找出“项目为什么在这台电脑上跑不起来”。

## 第 1 步：先确定第一版要做什么

第一版只做 3 件事：

- 记录：保存当前项目正常运行时的环境信息。
- 对比：检查另一台电脑的环境是否不同。
- 建议：用简单语言告诉用户缺什么、该怎么解决。

先不要做网页界面、自动安装软件、云同步、团队后台。这些会让项目变复杂。

## 第 2 步：准备开发环境

- [ ] 安装 Node.js，建议使用 22 或更高版本。
- [ ] 安装并能运行 DeepSeek Harness。
- [ ] 新建一个 GitHub 仓库，例如 `你的用户名/dsh-workspace-drift`。
- [ ] 在仓库中初始化一个 Node.js 项目。
- [ ] 给仓库添加 `dsh-plugin` topic 标签。
- [ ] 先阅读一个简单的 DSH 插件示例，了解 `dsh.bundle` 的写法。

“仓库”就是存放你插件代码的 GitHub 项目。  
“Node.js”是让 JavaScript 程序能在电脑上运行的工具。

## 第 3 步：设计用户怎么使用

用户只需要会说这几句话：

```text
记录这个项目的开发环境
检查当前电脑和项目环境是否一致
告诉我这个项目为什么启动失败
```

插件可以提供 3 个工具：

| 工具名 | 用户得到什么 |
|---|---|
| `environment_snapshot` | 记录当前项目环境 |
| `environment_check` | 检查现在的环境 |
| `environment_compare` | 和以前记录的环境作比较 |

## 第 4 步：决定要记录哪些信息

第一版只记录这些安全、容易获取的信息：

- [ ] Node.js 版本，例如 `22.14.0`
- [ ] Python 版本，例如 `3.12.8`
- [ ] 包管理工具，例如 npm、pnpm、yarn
- [ ] 项目使用的依赖文件，例如 `package-lock.json`、`pnpm-lock.yaml`
- [ ] Git 当前分支和最新提交编号
- [ ] 项目可用的启动、测试、构建命令
- [ ] 项目要求哪些环境变量，例如 `API_URL`
- [ ] 当前操作系统，例如 Windows、macOS、Linux

不要记录这些内容：

- [ ] API Key
- [ ] 密码
- [ ] Token
- [ ] `.env` 文件中的具体值
- [ ] 用户私密文件内容

正确做法是只记录“需要 `API_URL`”，不记录它真正的地址或密码。

## 第 5 步：先做“记录环境”

完成后，用户在一个正常项目中运行：

```text
记录这个项目的开发环境
```

插件在项目中创建一个文件，例如：

```text
.dsh/environment-baseline.json
```

里面大致是：

```json
{
  "node": "22.14.0",
  "packageManager": "pnpm 9.15.0",
  "requiredEnvironmentVariables": ["API_URL", "DATABASE_URL"],
  "scripts": ["dev", "test", "build"]
}
```

- [ ] 能正确读取 Node.js 版本。
- [ ] 能识别 npm、pnpm 或 yarn。
- [ ] 能读取 `package.json` 中的命令。
- [ ] 能识别 `.env.example` 中声明的变量名。
- [ ] 不把密钥写入记录文件。

## 第 6 步：再做“检查环境”

用户在另一台电脑上运行：

```text
检查当前项目环境
```

插件应该检查：

- [ ] Node.js 是否安装。
- [ ] Node.js 版本是否符合要求。
- [ ] 正确的包管理工具是否安装。
- [ ] 项目依赖是否已经安装。
- [ ] 必需配置是否缺失。
- [ ] 项目是否有可运行的启动命令。

输出要说人话，例如：

```text
发现 3 个问题：

1. 这个项目原本使用 Node.js 22，你当前使用的是 Node.js 18。
2. 项目需要 pnpm，但当前电脑没有安装。
3. 缺少 API_URL 配置。

建议：
- 安装 Node.js 22；
- 运行 corepack enable；
- 根据 .env.example 补充 API_URL。
```

## 第 7 步：做“环境对比”

让插件把“以前记录的环境”和“现在的环境”放在一起比较。

- [ ] 显示完全一致的项目。
- [ ] 显示版本不同的项目。
- [ ] 显示缺少的软件或配置。
- [ ] 按严重程度排序：无法启动的问题排在前面。
- [ ] 每个问题都给出下一步建议。

不要只输出技术错误，例如：

```text
Node version mismatch
```

要同时解释：

```text
Node.js 版本不一致：项目记录的是 22，但当前是 18。这可能导致依赖安装或构建失败。
```

## 第 8 步：补上 DSH 插件必需文件

在你的 `package.json` 中声明 DSH 插件信息。

- [ ] 有 `dsh.bundle`。
- [ ] 有对应的 `cordis.patch.yml`。
- [ ] 插件能通过 `dsh plugin add` 安装。
- [ ] 插件安装后能注册并调用上述工具。
- [ ] README 写清楚安装方式和使用示例。

这一项很重要：只有 `dsh.client` 不够，提交到插件列表会被拒绝；必须有 `dsh.bundle`。

## 第 9 步：自己测试

至少准备三种测试情况：

- [ ] 正常项目：环境相同，应显示“未发现关键差异”。
- [ ] Node.js 版本不同：应清楚提示版本问题。
- [ ] 缺少一个环境变量：应只显示变量名，绝不能显示密钥。
- [ ] 没有安装依赖：应提示执行对应的安装命令。
- [ ] 没有 `package.json` 的普通文件夹：应正常提示“未识别为 Node.js 项目”，不能崩溃。

## 第 10 步：准备提交到插件列表

你的插件仓库至少应具备：

- [ ] 真实可运行的代码，不是只有 README。
- [ ] 至少 10 次 Git 提交。
- [ ] 仓库创建超过 1 天。
- [ ] GitHub 添加 `dsh-plugin` topic。
- [ ] README 中写明功能、安装方式、隐私边界。
- [ ] 插件声明 `dsh.bundle`。

然后在当前这个插件列表项目中新增一份 YAML 文件：

```text
data/plugins/你的用户名__dsh-workspace-drift.yml
```

内容可先写成：

```yaml
url: https://github.com/你的用户名/dsh-workspace-drift
name: 你的用户名/dsh-workspace-drift
category: dev
description:
  en: Records a project's development environment and compares it on another machine, identifying missing tools, version differences, dependencies, and required configuration names without storing secret values.
  zh: 记录项目可运行时的开发环境，并在另一台电脑上进行对比，找出缺少的工具、版本差异、依赖和必需配置名称；不会保存密钥内容。
```

最后运行 README 生成脚本，并把新增 YAML 与自动更新的两个 README 一起提交。

## 完成标准

第一版完成，意味着用户能做到：

```text
在电脑 A 记录环境
→ 把项目交给电脑 B
→ 在电脑 B 运行检查
→ 得到明确的差异和修复建议
```

这已经是一个完整、有实际用途、适合投稿的插件。